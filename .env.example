# Required for production builds. Use the public HTTPS origin only: no path,
# query string, fragment, credentials, or trailing deployment subdirectory.
VITE_SITE_URL=https://your-production-domain.example

# Optional: run Playwright against an already deployed preview instead of the
# local `vite preview` server.
PLAYWRIGHT_BASE_URL=
